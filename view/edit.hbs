<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body bg-light">
      <div class="text-center description mb-2">
        <p class="fs-6 fw-lighter fst-italic">* 為必填欄位</p>
        <p class="fs-3">▼修改本筆支出▼</p>
      </div>
      <div class="col-md-6 m-auto input-data mb-2 ">
        <form action="/records/{{record._id}}?_method=PUT" method="post">
          <div class="form-group mb-2">
            <label class="form-label" for="name">名稱<span class="required text-danger">*</span>:</label>
            <input class="form-control" id="name" type="text" name="name" value="{{record.name}}" required>
          </div>
          <div class="form-group mb-2">
            <label class="form-label" for="createdAt">日期<span class="required text-danger">*</span>:</label>
            <input class="form-control" id="createdAt" type="date" name="createdAt" value="{{record.createdAt}}"
              required>
          </div>
          <div class="form-group mb-2">
            <label class="form-label" for="categoryId">類別<span class="required text-danger">*</span>:</label>
            <select class="form-select" name="categoryId" id="categoryId" required>
              <option value="0">請選擇</option>
              <option value="1" {{#if categoryIdOne}}selected{{/if}}>家居物業</option>
              <option value="2" {{#if categoryIdTwo}}selected{{/if}}>交通出行</option>
              <option value="3" {{#if categoryIdThree}}selected{{/if}}>休閒娛樂</option>
              <option value="4" {{#if categoryIdFour}}selected{{/if}}>餐飲食品</option>
              <option value="5" {{#if categoryIdFive}}selected{{/if}}>其他</option>
            </select>
          </div>
          <div class="form-group mb-2">
            <label class="form-label" for="amount">金額<span class="required text-danger">*</span>:</label>
            <input class="form-control" id="amount" type="number" name="amount" value="{{record.amount}}" required>
          </div>
          <div class="text-center">
            <button class="btn btn-primary btn-block ms-1" type="submit">修改支出</button>
            <a href="/" class="btn btn-outline-primary btn-block">返回</a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>